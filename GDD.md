# GDD (Game Design Document)
Project: Labyrinth (2D Rotating Maze)

> **이 문서의 역할**: "무엇을(What) / 왜(Why)" — 게임 디자인·콘텐츠·플레이어 경험·경제를 다룬다.
> 구현 방법(엔진·물리·코드)은 `TDD.md`, AI 작업 규칙은 `CLAUDE.md`를 본다.
> 기술 용어/수치를 본문에 박지 않는다(필요 시 TDD 해당 절을 참조 링크로 가리킨다).

| 항목 | 값 |
|---|---|
| 버전 | v2.0 (GDD/TDD 분리 · 설계 정합성 정정) |
| 최종 수정 | 2026-06-29 |
| 장르 | 2D 물리 퍼즐 (모바일 아케이드) |
| 플랫폼 | iOS / Android (세로 모드) |
| 현재 진척 | Phase 1~3 구현 완료, Phase 4+ 진행 예정 (`Task.md`) |

### 변경 이력
| 날짜 | 버전 | 내용 |
|---|---|---|
| 2026-06-29 | v2.0 | 기술/구현 영역을 `TDD.md`로 분리. P0 모순 정정(별/화폐 분리, 데드존·승패 우선순위 확정, 미로 회전 용어), 다중공 규칙·누락 섹션(스코프/난이도/튜토리얼/화면전이/KPI/리스크) 추가. |
| ~2026-03 | v1.x | 초기 기획 및 B방식 전환 반영(상세 `DevelopLog.md`). |

---

## 1. 게임 개요 (Overview)

* **한 줄 소개**: 화면을 드래그해 미로를 기울이고, **중력만으로** 공을 색깔에 맞는 탈출구로 굴려 보내는 한 손 물리 퍼즐.
* **핵심 경험(Core Experience)**: "기울이면 굴러간다"는 직관적 물리 + "어떻게 기울여야 모든 공을 동시에 보낼까"라는 두뇌 플레이의 결합.
* **주요 차별점(USP)**: 후반부의 **다중 색상 공(Multi-color Balls) + 색상 게이트** 기믹. 하나의 중력으로 서로 다른 색 공들을 각자의 목적지에 보내야 하는 고난도 공간 퍼즐(→ §5).
* **타겟 유저**: 캐주얼~미드코어 퍼즐 유저(전 연령). 짧은 세션·반복 도전·기록 경쟁을 즐기는 층.
* **레퍼런스 / 시장 포지셔닝**: **[TBD]** — 경쟁작(틸트 미로/볼 롤링 퍼즐) 2~3종 벤치마크 표를 채울 것. 차별 축은 "다중 색상 공 라우팅 퍼즐".

---

## 2. 코어 게임 루프 (Core Loop)

```
스테이지 진입 (고정 뷰로 맵 전체 조망)
      │
드래그로 미로를 기울임  ──►  중력으로 공이 굴러감  ──►  관찰/재조정 (실시간 반복)
      │
   결과 분기
      ├─ 승리: 모든 공이 제 색 Goal 도달 → 결과 화면(타임·별)
      └─ 패배: 공 하나라도 함정/장외 → 즉시 Fast Retry(같은 자리에서 재도전)
```

* 한 판은 **수 초~수십 초**의 짧은 호흡. 실패해도 로딩 없이 즉시 재시작되어 "한 번 더"의 쾌적한 반복을 만든다(구현: TDD §7).

---

## 3. 규칙 (Rules)

### 3.1 승리
* 맵의 **모든 공**이 각자의 색상에 맞는 Goal에 들어가야 클리어.
* 먼저 도착한 공은 파티클과 함께 **즉시 사라진다(비활성화)** → 남은 공의 경로를 방해하지 않는다.

### 3.2 패배
* 단 **하나의 공이라도** Hole·Spike에 닿거나 **플레이 영역 밖으로 이탈**하면, 다른 공의 상태와 무관하게 즉시 **Game Over → Fast Retry**.

### 3.3 승/패 동시 발생 — **패배 우선**
* 마지막 공이 골인하는 순간 다른 공이 죽으면 **패배로 처리**한다(끝까지 지키지 못한 것으로 간주). (구현 규칙·한계: TDD §7.6)

### 3.4 오답 Goal = 벽
* 색이 맞지 않는 Goal(예: 붉은 공 ↔ 푸른 Goal)은 골인으로 인정되지 않고 **단단한 벽**처럼 공을 튕겨낸다.

### 3.5 빠른 재시작 (Fast Retry)
* 실패 시 씬을 다시 로드하지 않고 **모든 것을 초기 상태로 즉시 되돌린다**: 공의 위치·운동량, 미로 기울기(중력·시점), 그리고 **동적 기믹 상태(열린 문/부서진 블록/눌린 스위치)** 와 사라졌던 공까지 전부 복원. (초기화 범위 전체·구현: TDD §7.5)

### 3.6 보상
* 클리어 **타임**에 따라 별 **1~3개** 획득(스테이지별 기준 시간은 데이터로 관리, 기본 ≤15s=3별 / ≤30s=2별 / 그 외 1별). 별로 다음 레벨 해금.
* ⚠️ **설계 메모**: 시간 보상은 "빠르게"를, 퍼즐은 "신중하게"를 유도해 충돌할 수 있다. 별 1개(=클리어) 자체를 진행의 1차 보상으로 두고, 2~3별은 숙련/재도전용 도전 과제로 포지셔닝한다.

---

## 4. 조작 & 시점 (Controls & Camera) — 경험 관점

* **조작감 목표**: 손가락 드래그에 미로가 **부드럽게(관성/감쇠 있는)** 따라오며, 중력은 **항상 화면 아래**로 유지되어 공이 어디로 떨어질지 직관적으로 예측된다. (실물 미로판을 손으로 기울이는 느낌.)
* **중요 — 무엇이 회전하는가**: 유저 눈에는 미로가 도는 것처럼 보이지만, 실제로는 **중력 방향과 카메라가** 회전한다. 미로 자체는 고정이다. (왜·수식: TDD §2 / 본 문서 전체에서 "미로를 회전"은 *체감*을 뜻함.)
* **시점**: 맵 전체를 한 화면에 담는 **고정 뷰**. 회전해도 모서리가 잘리지 않도록 넉넉한 줌(구현: TDD §9).
* **입력 안정성(경험)**: 비정상적으로 빠른 스와이프에도 공이 벽을 뚫지 않도록 회전 변화에 상한을 둔다(체감상 "아무리 빨리 휘둘러도 깨지지 않음"). 멀티터치/일시정지 버튼 터치 시 회전 오작동 없음. (구현: TDD §2.4, §3)

---

## 5. 다중 색상 공 시스템 (USP)

### 5.1 개념
* 각 공과 Goal·게이트는 **색상 속성**을 가진다(Red/Blue/Green/Yellow 등). 색이 맞아야 골인하고, 색상 게이트는 특정 색만 통과시킨다.

#### 5.1.1 색상 게이트 방향성 — **확정: 같은 색 통과 / 다른 색 차단** (2026-07-06)
* **규칙(1차·기본값)**: 게이트와 **같은 색 공은 통과**, **다른 색 공은 차단(벽처럼 튕김)**. 게이트 색 = "이 색만 지나갈 수 있는 문"으로 읽힌다.
* **직관 근거**: ①색=소속/친화라는 보편 관습, ②Goal의 "같은 색=수용"과 규칙을 일원화("네 색은 네 편, 같은 색은 열린다"는 단일 규칙으로 골·게이트에 일관 적용). "같은 색 차단"은 골과 정반대 의미가 되어 인지 부조화를 일으키므로 채택하지 않는다.
* ⚠️ **비주얼 필수 조건**: 직관성은 렌더링에 의존한다. 게이트는 **통과 가능해 보이는 형태**여야 한다 — 색깔 에너지 장막/반투명 커튼/뚫린 색깔 프레임(❌ 불투명 색깔 블록은 "벽"으로 오독됨).
* **색맹 대응**: 게이트에 해당 색의 **고유 문양**을 각인(예: Red=별)해 색+모양 이중으로 "이 색의 문"을 전달(§9 접근성과 연동).
* **트레이드오프(후순위 도구)**: "같은 색 **차단**" 게이트도 유효한 별도 퍼즐 도구지만, 기본값으로 쓰지 않는다. 도입 시 통과형과 **시각적으로 확실히 구분**(예: 통과형=뚫린 프레임 / 차단형=꽉 찬 색 바)하고 후반 레벨에서 별도로 학습시킨다.
* **구현**: 통과/차단은 **Layer Collision Matrix**로 처리(`Gate_X`가 `Ball_X`만 무시=통과, 나머지 공과 충돌). 정체성 판정이 아닌 물리 필터이므로 코드가 아닌 레이어. → TDD §5.2.

### 5.2 설계 제약 — **하나의 중력을 공유**
* 모든 공은 **같은 중력**의 영향을 받는다. 즉 한 입력으로 한 공만 따로 다른 방향으로 보낼 수 없다.
* 따라서 난이도는 "**하나의 기울임으로 여러 공의 경로를 동시에 만족**"시키는 데서 나온다. 차이를 만드는 도구는 **색상 게이트·지형·타이밍**이지 개별 중력이 아니다. (이 제약을 전제로 레벨을 설계한다.)

### 5.3 공끼리의 상호작용 — **확정: 모든 공이 서로 충돌**
* 색에 관계없이 **모든 공이 물리적으로 충돌(통과 불가)** 한다. 서로 밀고 부딪히는 핀볼식 상호 간섭이 퍼즐의 변수가 된다.
* **레벨 설계 유의**: 한 공의 움직임이 다른 공을 밀어 경로·타이밍에 영향을 준다. 좁은 통로에서 서로 끼여 못 빠져나가는 불공정한 교착이 생기지 않도록 정답 경로에 여유 폭을 둔다.
* (구현: TDD §5.3)

### 5.4 좋은 멀티볼 레벨의 원칙
1. **단일 해법의 긴장**: 한 공에 유리한 기울기가 다른 공에는 불리하도록 배치해 "절충 각도"를 찾게 한다.
2. **게이트로 분기**: 색상 게이트로 같은 통로를 색마다 다르게 쓰게 한다.
3. **순서 설계**: 어떤 공을 먼저 보내야 다른 공의 길이 열리는 선후 관계.
4. **안전 마진**: 함정·장외는 "실수의 대가"이지 "운빨"이 되지 않도록, 정답 경로에 충분한 여유를 둔다.

---

## 6. 레벨 디자인 (Level Design)

### 6.1 레벨 요소 카탈로그
| 요소 | 역할 | 비고 |
|---|---|---|
| **Wall(벽)** | 일반 충돌 지형 | 정적 지형, 곡선/대각 지형 가능 |
| **Goal(탈출구)** | 색 일치 공만 골인, 오답은 벽 | 색상 속성 보유 |
| **Hole / Spike** | 닿으면 즉시 실패 | 의도된 함정 |
| **Dead Zone(장외)** | 미로 바깥을 두른 **외곽 킬 영역**. 공이 벽을 뚫고 이탈해 이 영역에 **진입하면 실패** | 소프트락 방지용 안전장치(구현/배치 제약: TDD §7.4) |
| **Color Gate(색상 게이트)** | 특정 색만 통과/차단 | 멀티볼 퍼즐의 핵심 |
| **Switch & Door** | 스위치를 먼저 친 뒤 막힌 경로가 열림 | 동적 기믹(Phase 4+) |
| **Fragile Block** | 일정 속도 이상 충돌 시 부서지는 벽 | 타격감, 동적 기믹 |
| **Bumper(범퍼)** | 닿으면 강하게 튕겨내는 핀볼식 탄성 | 동적 기믹 |

### 6.2 콘텐츠 스코프 (Scope) — **의도적으로 미확정 (개발하며 조정)**
* 총량을 지금 고정하지 않는다. **데이터 주도(`StageData` ScriptableObject)로 레벨을 점진적으로 추가·조정**하는 확장 구조를 먼저 갖추고, 플레이 감을 보며 분량을 늘린다. → 기본 게임 + 소수 스테이지부터(MVP→확장: §11).
* 아래는 *방향 감각용 잠정치*이며 약속이 아니다.

| 구분 | 잠정 방향 | 비고 |
|---|---|---|
| 월드 수 | 3~5 | 테마/난이도 묶음 |
| 월드당 레벨 | 10~15 | |
| 총 레벨(출시) | 미정 | 개발하며 결정 |
| 예상 1회 세션 | 3~10분 | |

### 6.3 난이도 곡선 & 메커닉 도입
* **원칙: 한 번에 하나의 새 메커닉.** 새 요소는 안전한 환경에서 1회 학습 → 1~2레벨 응용 → 기존 요소와 조합.
* 대략적 페이싱:
  * **초반(학습)**: 단일 공. 회전 조작·가속/마찰 적응, 단순 함정.
  * **중반**: 색상 게이트, 다중 공 2개 도입.
  * **후반(응용)**: 다중 공 + 스위치/문/범퍼/파괴블록 조합, 선후 관계 퍼즐.
* 구체적 레벨별 도입 스케줄 표는 **[TBD]**.

### 6.4 튜토리얼 / 첫 사용자 경험(FTUE)
* **무텍스트 지향**: 첫 1~3레벨에서 손가락 드래그→공 굴림→골인을 *행동으로* 학습. 필요한 곳에만 미니 손가락 힌트.
* 새 기믹 첫 등장 시 1회 비차단 툴팁. (상세 와이어프레임 **[TBD]**.)

---

## 7. 진행 · 메타 · 경제 (Progression & Economy)

### 7.1 별(Stars) — 성취 기록 (불변)
* 각 레벨에서 받은 별(1~3)은 **성취 기록**이다. 월드맵에 표시되며 **소모되지 않는다**(누적 총합은 진행 게이트로 사용 가능).

### 7.2 소프트 화폐(Soft Currency) — 소비재 ★별과 분리★
* 스킨 구매 등에 쓰는 **소비 재화는 별과 별개**의 화폐(예: Coin/Shard)로 둔다.
* 사유: 별(성취)을 소모하면 월드맵 등급이 깎이고 "광고로 등급을 사는" 모순이 생긴다 → **평가용 별 ↔ 소비용 화폐를 분리**한다.
* 화폐 획득: 레벨 클리어 보상, 일일/도전 보상 등.

### 7.3 스킨 시스템(보상)
* 화폐로 공의 궤적(Trail)·골인 파티클·미로 테마(스킨) 등 **시각 보상**을 해금. 게임플레이 능력에 영향 없음(순수 코스메틱).

### 7.4 수익화(BM)
* 흐름을 끊는 강제 전면 광고 **지양**. 주력은 **보상형 광고(Rewarded Ad)**: "화폐가 부족할 때 동영상 시청 → **화폐**(별 아님) 획득" 형태.
* 추가 IAP(광고 제거/화폐 팩) **[TBD]**.

### 7.5 저장
* 별 기록·클리어 타임·해금·보유 화폐·구매 스킨을 로컬에 저장(개념). 구현·스키마·암호화: TDD §10.

---

## 8. UI/UX 플로우 (UI Flow)

### 8.1 화면 전이도
```
[Splash] → [메인 로비/월드맵] ──► [스테이지 선택]
   로비 ├─► [상점(스킨)]
        └─► [설정]
[스테이지 선택] → [인게임] ⇄ [일시정지]
[인게임] → [결과(Results)] ─┬─► [다음] → 인게임
                            ├─► [재시작] → 인게임
                            └─► [로비]
```

### 8.2 주요 화면
* **메인 로비/월드맵**: 스테이지 선택, 누적 별·달성도 표시.
* **인게임 HUD**: 타이머, 일시정지 버튼(최소 UI로 몰입 유지).
* **결과(Results)**: 최종 타임, 획득 별 랭크, [재시작]/[다음]/[로비].
* **상점/설정**: 스킨 구매, 사운드/햅틱/접근성 토글.

### 8.3 접근성 (Accessibility)
* **색맹/색약 지원**: 색상으로만 구분하지 않는다. **공·Goal·색상 게이트** 각각에 고유 문양을 각인(예: Red=별, Blue=세모)하고, 색약 모드 토글 제공.
* **모션 완화(Reduce Motion)**: 코어가 상시 화면 회전이라 멀미 위험이 있다. **회전 강도 완화/감속 옵션**(또는 카메라 회전 폭 축소)을 설정에 제공. *(우선 검토 필요 — §11 리스크)*
* **입력/사운드 토글**: 햅틱 ON/OFF, 사운드 볼륨, 한 손 조작 고려.

### 8.4 현지화(Localization)
* 1차: 한국어 / 영어. 텍스트는 외부 테이블로 분리. 무텍스트 튜토리얼 지향으로 번역량 최소화. (대상 언어 확정 **[TBD]**.)

---

## 9. 오디오 & 피드백 (Audio & Feedback) — 경험

* **충돌**: 공이 벽에 부딪힐 때 충격량에 비례한 **약한 햅틱 + 둔탁한 SFX**. 연속 충돌로 인한 진동/사운드 폭주를 막는 짧은 쿨타임을 둔다(구현: TDD §8).
* **클리어**: 골인 시 경쾌한 성공음 + 파티클.
* **BGM 디렉션**: 차분하고 집중을 돕는 루프. 회전/속도에 따른 미묘한 사운드 변화는 후반 폴리싱(Task Phase 6). 구체 톤·레퍼런스 **[TBD]**.

---

## 10. 지표 & KPI (Analytics)

* **핵심 KPI**: 리텐션(D1/D7), 레벨별 **실패율·이탈률**, 평균 클리어 타임, 광고 시청률, 스킨 구매 전환.
* **분석 이벤트(예시)**: `level_start`, `level_clear(time, stars)`, `level_fail(reason: hole/spike/oob)`, `retry_count`, `rewarded_ad_view`, `skin_purchase`.
* **활용**: 실패율이 비정상적으로 높은 레벨 = 난이도 스파이크 → 리밸런싱 신호.
* 도구·파이프라인(Firebase 등) **[TBD]**.

---

## 11. 스코프 우선순위 & 리스크 (Scope & Risk)

### 11.1 MVP 정의 (먼저 완성할 것)
1. **단일 공 코어 루프**: 회전 조작 → 골인/실패 → Fast Retry → 별 평가. *(구현 완료)*
2. **소량의 레벨(예: 1개 월드)** + 로비/결과 화면.
3. 그 위에 **다중 색상 공(USP)** → 메타(스킨/화폐) → 폴리싱/최적화 순으로 확장.

### 11.2 리스크 레지스터
| 리스크 | 영향 | 대응 |
|---|---|---|
| **회전 멀미** | 코어 메커닉이 불편을 유발하면 이탈 | 모션 완화 옵션(§8.3) 조기 프로토타입·실기 테스트 |
| **멀티볼 난이도 폭증** | 학습 곡선 붕괴 | 메커닉 1개씩 도입(§6.3), 실패율 KPI 모니터링 |
| **색상 레이어 예산** | 색 추가 시 기술 한계 | 하이브리드 매칭(TDD §5.2) |
| **콘텐츠 제작 비용** | 레벨 수 = 일정 | 레벨 오소링 파이프라인 효율화(TDD §6), 스코프 단계화 |
| **별/화폐 경제 설계 오류** | BM 신뢰 훼손 | 별↔화폐 분리(§7) 선반영(완료) |

### 11.3 일정/마일스톤
* `Task.md`의 Phase 1~7로 관리. 본 GDD는 "무엇을", Task는 "언제/순서".

---

## 부록 A. 용어집 (Glossary)

| 용어 | 의미 |
|---|---|
| **B방식** | 미로를 실제로 돌리지 않고 중력+카메라를 회전시키는 착시 구현(TDD §2). |
| **Fast Retry** | 씬 재로드 없이 즉시 초기 상태 복원하는 재시작(TDD §7). |
| **Color Gate** | 특정 색 공만 통과시키는 게이트. |
| **Dead Zone** | 미로 외곽의 장외 이탈 감지 영역(진입=실패). |
| **별(Stars)** | 클리어 성취 등급(불변 기록). |
| **소프트 화폐** | 스킨 구매용 소비 재화(별과 분리). |
| **USP** | 다중 색상 공 라우팅 퍼즐. |
